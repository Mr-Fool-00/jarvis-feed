# Briefing: github-mcp-server v1.6.0 — Token-Efficient Fields Parameter

**Date:** 2026-07-16  
**Score:** 7/10  
**ID:** github:github-mcp-server-v1.6.0-2026-07  
**URL:** https://github.com/github/github-mcp-server/releases/tag/v1.6.0  
**Released:** 2026-07-15T13:03Z

---

## What's new

**Token-efficient `fields` parameter** added to 8 tools:
- `search_code`
- `get_file_contents`
- `list_issues`, `search_issues`
- `list_pull_requests`, `search_pull_requests`
- `list_commits`, `search_commits`

You can now request only the fields you need (e.g., `fields=["number","title","state"]`) instead of getting the full response object. For tools that return large lists, this can cut output token costs significantly.

**CSV output mode** added in Insiders build — structured tabular output for list tools.

**go-sdk upgraded to v1.7.0-pre.1** — aligns with the incoming MCP 2026-07-28 spec RC (breaking changes). This update positions github-mcp-server to be spec-compliant before the RC lands.

## Why it matters

Jarvis uses github-mcp-server in every run (list_issues, search_code, get_file_contents). The `fields` parameter directly reduces the token cost of those calls. At Claude Max pricing, every token saved is real money.

The go-sdk alignment is also important: when the July 28 MCP RC drops, github-mcp-server v1.6.0 should be compatible. Staying on an older version might break things.

## Step 4.5 assessment

**GitHub-official release** — this is not an arbitrary third-party MCP. Risk level: low. Still: Leo controls timing of any MCP update. Don't auto-update; confirm before upgrading the Jarvis environment.

## Action items

- [ ] When you next have a Jarvis maintenance window, upgrade github-mcp-server to v1.6.0
- [ ] Test `fields` parameter on `search_code` in a Jarvis discovery run — measure token delta
- [ ] Upgrade before July 28 MCP RC to stay spec-compatible

---

*Briefing generated by Jarvis Discovery Loop · PM run 2026-07-16*
